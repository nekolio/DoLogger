[package]
name = "dologger-plugin-formatter-text"
version = "0.1.0"
edition = "2021"
license.workspace = true
description = "DoLogger official plugin: human-readable colored text formatter with configurable columns"

[lib]
name = "formatter_text"
crate-type = ["rlib"]

[dependencies]
dologger-core = { path = "../../../core" }
