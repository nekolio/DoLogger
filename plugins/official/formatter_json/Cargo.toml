[package]
name = "dologger-plugin-formatter-json"
version = "0.0.1"
edition = "2021"
license.workspace = true
description = "DoLogger official plugin: structured JSON formatter with configurable field inclusion"

[lib]
name = "formatter_json"
crate-type = ["rlib"]

[dependencies]
dologger-core = { path = "../../../core" }
serde_json = "1"
